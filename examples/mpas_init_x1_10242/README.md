# MPAS init x1.10242

This case consumes a validated WPS `FILE:` product and creates one MPAS initial-condition file per cycle.

The case requires reviewed `namelist.init_atmosphere` and `streams.init_atmosphere` templates from the installed MPAS revision.
