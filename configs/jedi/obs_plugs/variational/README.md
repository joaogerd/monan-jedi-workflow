# Variational observation plugs

Place JEDI observer stubs here. The original workflow uses `config/jedi/ObsPlugs/variational/*.yaml`
and fills the `observers` section through `PrepJEDI.csh`.

For the first JACI smoke test, prefer offline IODA conventional observations only.
