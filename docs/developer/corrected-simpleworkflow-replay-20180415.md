# Corrected simpleWorkflow replay — 2018-04-15 00Z→18Z

## Status

Validated end-to-end on JACI on 2026-09-11 using `simpleWorkflow` as the actual orchestration engine over the `monan-jedi-workflow` domain stages.

Scope:

```text
JEDI00
  |
  +--> MPAS00->06 ----+
  |                    |
  +--> Obs2IODA06 -----+--> JEDI06
                            |
                            +--> MPAS06->12 ----+
                            |                    |
                            +--> Obs2IODA12 -----+--> JEDI12
                                                     |
                                                     +--> MPAS12->18 ----+
                                                     |                    |
                                                     +--> Obs2IODA18 -----+--> JEDI18
```

The workflow deliberately omits `Obs2IODA00` and terminates at `JEDI18`; there is no `MPAS18->00` stage in this reference replay.

## Final simpleWorkflow state

All 50 tasks finished with persisted state `success`, from `jedi00_prepare` through `jedi18_gate`.

Every stage validation manifest used by the DAG finished with `valid=true`:

- JEDI00
- MPAS00
- Obs2IODA06
- JEDI06
- MPAS06
- Obs2IODA12
- JEDI12
- MPAS12
- Obs2IODA18
- JEDI18

This demonstrates that scheduler completion alone did not release downstream tasks; the explicit validation gates were crossed only after the domain-stage validation manifests were valid.

## PBS job ledger

| Stage | Job ID | Persisted stage state | Scheduler terminal state |
| --- | --- | --- | --- |
| JEDI00 | `471013.pbs-ha` | `validated` | `F` |
| MPAS00→06 | `471205.pbs-ha` | `scheduler-finished` | `F` |
| JEDI06 | `471272.pbs-ha` | `validated` | `F` |
| MPAS06→12 | `471283.pbs-ha` | `scheduler-finished` | `F` |
| JEDI12 | `471289.pbs-ha` | `validated` | `F` |
| MPAS12→18 | `471295.pbs-ha` | `scheduler-finished` | `F` |
| JEDI18 | `471301.pbs-ha` | `validated` | `F` |

`F` above is the PBS terminal scheduler state recorded by the stage watcher. Scientific success is established separately by the validation manifests.

## State-schema contract

The replay reproduced the known MPAS state-schema transition:

- 00Z analysis: 62 variables, no `refl10cm`;
- MPAS06 and every later analysis/base state: 63 variables, including `refl10cm`.

For this already-materialized replay, the 06/12/18 analysis initialization manifests recorded the compatibility rule `legacy-cycling-refl10cm`. The corrected materializer now writes the explicit contract for newly materialized replays:

```yaml
expected_variable_count:
  first_cycle: 62
  cycling: 63
```

The complete-state initialization manifests reported `state=copied` at 00/06/12/18Z.

## Cross-cycle handoffs

The audited symlinks show that later-cycle JEDI stages consumed only products generated inside the clean replay namespace:

- JEDI06 background trajectory -> MPAS00 03Z;
- JEDI06 observations -> replay Obs2IODA06 products;
- JEDI12 background trajectory -> MPAS06 09Z;
- JEDI12 observations -> replay Obs2IODA12 products;
- JEDI18 background trajectory -> MPAS12 15Z;
- JEDI18 observations -> replay Obs2IODA18 products.

The analysis-time complete base states are also generated by the immediately preceding replay MPAS legs and are used by the JEDI complete-state initialization mechanism.

No 06/12/18 JEDI, MPAS or IODA product from the earlier manually validated reference campaign is used as a replay intermediate.

## Scientific product SHA-256 ledger

| Product | SHA-256 |
| --- | --- |
| JEDI00 analysis | `893250735ad9b9819250f7a1bc288995166833958e195d902de4d08a51200624` |
| MPAS03 | `d3f7a44edadf6cfa888b5619a66c68c4574b660ffad744588d9a8cb700fcc0ff` |
| MPAS06 base | `ca929007eb3bb15f9a3a9311d9419ed8d4dad1cdecd462c8f01563e442828208` |
| JEDI06 analysis | `a602bc19bb6d546f3d088dba7aeac1ec19b74db9b8a65ab91eb949ff57ac3a0d` |
| MPAS09 | `f19599ef768c17266382a6f58a94cb99207ac18f8c848cbc1f8be73f4a834d2b` |
| MPAS12 base | `cfdeea2751df50542dc7fa71710da6c438c48aceac9b48f20280fb9c6bb69adb` |
| JEDI12 analysis | `dd68affc118ab598e68706bd1d1bb195be6a1df359d816d28625ed4ccb788e90` |
| MPAS15 | `a0752f9388a79c553dc09f1097226fbebb941fccc02261511267a3b4e0e6d0d1` |
| MPAS18 base | `d4f15a35f9f2e5f3822fc0e63ff6a418c42d005398cee42c8773b1df2fd31924` |
| JEDI18 analysis | `af388684b4999ceb7ca9ef61eda5114f122c71620accf1609a01668d200a5c95` |

Bitwise identity with the earlier manually orchestrated campaign is not an acceptance requirement. These hashes are the immutable ledger for this simpleWorkflow replay.

## Failure and restart evidence

The campaign also exercised restart behavior in real operation.

### MPAS00 bootstrap failure

The first MPAS00 PBS submission (`471015.pbs-ha`) failed before the model started because the historical source case contributed an obsolete setup script path:

```text
/p/projetos/monan_das/joao.gerd/projects/mpas-bmatrix-global/scripts/load_jaci_env.sh
```

The job terminated before producing MPAS logs or scientific outputs. The workflow stopped at `mpas00_validate` and did not release downstream stages.

The replay materializer was corrected so newly materialized corrected replays do not inherit stale `mpas.pbs.setup` bootstrap scripts. The successful JEDI00 state was preserved and only the MPAS00 task block was reset/replayed.

### JEDI06 state-schema guard

The next interruption occurred at `jedi06_prepare`, before any PBS submission, when the inherited 00Z scalar state-count contract expected 62 variables but the replay MPAS06 state correctly contained 63 variables including `refl10cm`.

The JEDI stage and corrected replay materializer were updated to encode the first-cycle/cycling distinction explicitly while retaining compatibility for the already-materialized replay.

After this correction, the same workflow invocation reused every successful upstream task and resumed from JEDI06 through JEDI18.

These two interruptions demonstrate that the orchestration stops on invalid operational/scientific contracts and can resume without unnecessarily repeating validated upstream work.

## Tests supporting the orchestration contract

`monan-jedi-workflow` tests cover:

- explicit cross-cycle DAG dependencies;
- omission of `Obs2IODA00` and `MPAS18->00`;
- validation gates with SHA-256 input fingerprinting;
- `valid=true` gate behavior and rejection of false/missing/invalid manifests;
- clean replay namespace materialization;
- removal of stale historical MPAS PBS setup;
- explicit 62/63 state-count contract in newly materialized corrected replay cases.

The `simpleWorkflow` test suite independently covers persisted-state reuse based on task signatures and rerun when a required output disappears. The JACI campaign provides the corresponding operational restart evidence for this scientific DAG.

## Conclusion

The orchestration limitation previously documented after the manual corrected campaign is resolved for the validated 2018-04-15 00Z→18Z reference scope.

`simpleWorkflow` now provides a faithful corrected multi-cycle orchestration in which:

- cross-cycle dependencies are explicit;
- domain stages retain ownership of JEDI/MPAS/Obs2IODA mechanics;
- downstream work is gated on scientific/runtime validation, not only scheduler completion;
- the replay uses a clean namespace and regenerated intermediate products;
- restart preserves validated upstream work and resumes from the failed/changed stage;
- the reference scope terminates exactly at JEDI18.

Future work should extend the orchestration beyond this fixed reference campaign only after this reference behavior remains preserved by regression tests.